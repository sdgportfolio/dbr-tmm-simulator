# dbr-tmm-simulator
A simulation for distributed Bragg reflectors using the transfer matrix method (TMM) to compute the reflectance and transmission spectra and study how stopband width and peak reflectance depend on layer count. Validated against Tidy3D FDTD simulation.
